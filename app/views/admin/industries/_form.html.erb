<%= form_with(model: admin_industry) do |form| %>
  <% if admin_industry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_industry.errors.count, "error") %> prohibited this admin_industry from being saved:</h2>

      <ul>
        <% admin_industry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
